
<% if @crypto %>
    <h1><%= @crypto.name %> - $
    <%= @crypto.price %> USD</h1>
    <h3><%= @crypto.description %></h3>
    <h1><%= link_to "Make a New Trade", new_crypto_trade_path(@crypto), class:"btn btn-primary btn-lg" %></h1><br /><br />
    <h4>Recent <%= @crypto.name %> Trades</h4>
<% else %>
    <h3>Recent Trades</h3>
<% end %>

<%= render partial: "trades" %>



    
    
    
