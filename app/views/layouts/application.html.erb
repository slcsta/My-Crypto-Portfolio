<!DOCTYPE html>
<html>
  <head>
    <title>MyCryptoPortfolio</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div id="nav">
  <% if logged_in %>
    <%= link_to "Crypto Market", markets_path %> |
    <%= link_to "Make a Trade", cryptos_path %></li> |
    <%= link_to "My Trades", user_path(current_user)%></li> |
    <%= link_to "Last 10 Trades", trades_path %></li> |
    <%= link_to "Log Out", logout_path, method: :delete %></li>
  <% end %>
</div>
  <div class="wrapper">

  <% flash.each do |type, msg| %>
    <%= msg %>
  <% end %>
    
    <%= yield %>
  
    </body>
</html>
